class Solution(object):
    def peakIndexInMountainArray(self, arr):
        # left =0 
        # right = len(arr)-1
        # while left<=right:
        #     mid= (left+right)//2
        #     if( )
        x= max(arr)
        return arr.index(x)
        